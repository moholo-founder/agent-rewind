/**
 * @backstop/api — Express + SSE server for the timeline UI.
 *
 * M0 stub. M5 adds: GET /timeline (SSE live + history), POST /undo/:actionId,
 * POST /rewind, POST /stop, POST /resume, POST /hold/:actionId/approve|reject.
 */

export const API_PACKAGE = "@backstop/api";
