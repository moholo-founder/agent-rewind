/**
 * @backstop/connectors — capability manifests & compensators.
 *
 * M0 stub. M2 adds the sandboxed filesystem connector (capture/undo for
 * write/move/delete, blast radius, symlink-escape guard); M4 adds the
 * self-contained mock email MCP server with delayed-outbox send recall.
 */

export const CONNECTORS_PACKAGE = "@backstop/connectors";
